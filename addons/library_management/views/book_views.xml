<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="action_library_book" model="ir.actions.act_window">
            <field name="name">Libros</field>
            <field name="res_model">library.book</field>
            <field name="view_mode">tree,form</field>
        </record>
        <menuitem
            id="library_menu_books"
            name="Libros"
            parent="library_menu_root"
            action="action_library_book"
            sequence="30"
        />
        <record id="view_book_tree" model="ir.ui.view">
            <field name="name">library.book.tree</field>
            <field name="model">library.book</field>
            <field name="arch" type="xml">
                <tree>
                    <field name="title" string="Titulo" />
                    <field name="author" string="Autor" />
                    <field name="isbn" string="ISBN" />
                    <field name="publication_date" string="Publicado en" />
                    <field name="years_since_publication" string="Años desde su publicación" />
                    <field name="active" string="Disponible" widget="boolean_button" />
                </tree>
            </field>
        </record>
        <record id="view_book_form" model="ir.ui.view">
            <field name="name">library.book.form</field>
            <field name="model">library.book</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <group>
                            <field name="title" />
                            <field name="author" />
                            <field name="isbn" />
                            <field name="publication_date" string="Año de publicación" widget="integer"/>
                            <field name="years_since_publication" readonly="1" />
                        </group>
                    </sheet>
                </form>
            </field>
        </record>
        <record id="view_book_search" model="ir.ui.view">
            <field name="name">library.book.search</field>
            <field name="model">library.book</field>
            <field name="arch" type="xml">
                <search>
                    <field name="title" />
                    <field name="author" />
                    <field name="isbn" />
                    <filter name="older_than_5" string="+5 años"
                        domain="[('years_since_publication','>',5)]" />
                    <filter name="available" string="Disponibles" domain="[('active','=',True)]" />
                </search>
            </field>
        </record>
    </data>
</odoo>